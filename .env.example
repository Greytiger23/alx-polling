# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Supabase Project URL (found in your Supabase project settings)
NEXT_PUBLIC_SUPABASE_URL='https://oougcrhzcpnffuibunli.supabase.co';

# Supabase Anon Key (found in your Supabase project API settings)
NEXT_PUBLIC_SUPABASE_ANON_KEY=process.env.SUPABASE_KEY || '';

# Supabase Service Role Key (found in your Supabase project API settings)
# This is used for server-side operations and should be kept secret
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Application Configuration
# The base URL of your application (used for generating poll URLs and QR codes)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: JWT Secret (if you need custom JWT handling)
# JWT_SECRET=your_jwt_secret

# Optional: Database URL (if you need direct database access)
# DATABASE_URL=your_database_connection_string